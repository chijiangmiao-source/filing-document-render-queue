# Host port exposed for the FastAPI service (container port is always 8000).
API_PORT=8080
# Everything below is optional; defaults live in app/config.py.
# DATABASE_URL=sqlite:////data/docx2pdf.db
# STORAGE_DIR=/data/storage
# LEASE_SECONDS=30
# RENEW_INTERVAL_SECONDS=10
# MAX_ATTEMPTS=3
# MAX_UPLOAD_BYTES=10485760
